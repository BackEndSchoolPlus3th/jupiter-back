server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://172.17.0.1:3306/wyl_prod
    username: root
    password: root123414
    driver-class-name: com.mysql.cj.jdbc.Driver
custom:
  site:
    name: "WYL"
    cookieDomain: "${custom.prod.cookieDomain}"
    frontUrl: "${custom.prod.frontUrl}"
    backUrl: "${custom.prod.backUrl}"