name: exam_db
services:
  mariadb:
    image: mysql:9.1.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123414
      MYSQL_DATABASE: exam_db
    ports:
      - "3306:3306"
    volumes:
      - ./db/:/var/lib/mysql